# Log Prompt - 102022400033_schedule-service

Dokumen ini berisi narasi log *prompt* (instruksi) yang digunakan untuk menyelesaikan pengembangan *backend API service* penjadwalan (*Schedule Service* dengan NIM `102022400033`). Pekerjaan ini hanya berfokus pada penyediaan rute REST API fungsional.

### 1. Setup Proyek API
"Buatkan kerangka proyek awal Laravel untuk layanan Schedule Service (Layanan Jadwal Penugasan Driver). Jangan lupa atur konfigurasi akses *database* MySQL di dalam file `.env`."

### 2. Pembuatan Database Migration
"Tolong buatkan file *migration* dan *model* untuk tabel Jadwal (`Schedules`). Buatkan kolom-kolom standar yang diperlukan seperti: id, vehicle_id, driver_id, destination, dan status penugasan."

### 3. Pembuatan Fungsi CRUD API
"Bantu saya membuat *Controller* yang berfungsi murni untuk melayani permintaan (*request*) API jadwal. Sediakan fungsi GET untuk membaca daftar jadwal, POST untuk mencatat jadwal penugasan baru, dan PATCH untuk memperbarui status jadwal driver. Pasang rute API-nya agar dapat diakses pada `/api/v1/schedules`."
